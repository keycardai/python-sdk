[project]
name = "oauth-client-usage"
version = "0.1.0"
description = "Example: AgentClient with PKCE user authentication"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "keycardai-agents",
]

[tool.uv.sources]
keycardai-agents = { path = "../../", editable = true }

[project.scripts]
oauth-client-usage = "main:run"
