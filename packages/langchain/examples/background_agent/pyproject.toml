[project]
name = "background-agent-example"
version = "0.1.0"
description = "A background LangChain agent acting as itself, with credentials brokered by Keycard"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "keycardai-langchain",
    "langchain-anthropic>=1.0",
    "httpx>=0.27.0,<1.0.0",
]

[tool.uv.sources]
keycardai-langchain = { path = "../../", editable = true }
